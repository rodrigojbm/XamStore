@model IEnumerable<XamStore.Domain.Entities.Cadastro.Console>
@{
    ViewBag.Title = "Cadastro de Console";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
@Html.Partial("MenuAdmin");
<div class="content-right">
    <h2>Cadastro de Console</h2>
    <p>
        @Html.ActionLink("Nova Console", "Cadastrar")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.ActionLink("Editar", "Editar", new { id = item.Id }, null) |
                    @Html.ActionLink("Deletar", "Deletar", "Console", new { id = item.Id }, null)
                </td>
            </tr>
        }
    </table>
</div>